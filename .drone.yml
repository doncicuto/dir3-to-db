kind: pipeline
type: docker
name: json-to-db

steps:
- name: submodules
  image: alpine/git
  commands:
  - git submodule init
  - git submodule update
- name: import-to-db
  image: node:lts
  volumes:
  - name: input
    path: /drone/src/src/utils/filesToImport
  environment:
    DATABASE_URL:
      from_secret: database_url
  commands:
  - yarn
  - yarn start

volumes:
- name: input
  host:
    path: /tmp/dir3-json
